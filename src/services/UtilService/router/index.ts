import { Router } from "express";
import { utils } from "../controller/utils";
// import { validateSchema } from "../../../common";

const utilRouter = Router();

// utilRouter.get("/utils", utils);

export default utilRouter;
